---
phase: 07-polish-deploy
plan: 09
type: execute
wave: 2
depends_on: ["07-05"]
files_modified:
  - frontend/src/components/dashboard/index.tsx
  - frontend/src/components/dashboard/AdminDashboard.tsx
  - frontend/src/components/training/TrainingHeader.tsx
  - frontend/src/components/training/TrainingRecommendations.tsx
  - frontend/src/components/training/LoadAdjustments.tsx
  - frontend/src/components/training/WorkoutCalendar.tsx
  - frontend/src/components/wellbeing/index.tsx
  - frontend/src/components/wellbeing/WellbeingCalendar.tsx
  - frontend/src/components/check-in/index.tsx
  - frontend/src/components/performance/index.tsx
  - frontend/src/components/charts/TrendCharts.tsx
  - frontend/src/components/settings/Settings.tsx
  - frontend/src/components/feedback/DailyFeedbackForm.tsx
  - frontend/src/app/profile/page.tsx
autonomous: false

must_haves:
  truths:
    - "All primary screens are usable on a 375px wide mobile viewport"
    - "Touch targets are at least 44px on all interactive elements"
    - "Data tables and charts reflow or scroll horizontally on mobile"
    - "Forms are thumb-friendly with appropriate input sizes"
    - "Navigation works on mobile (hamburger menu or bottom nav)"
  artifacts:
    - path: "frontend/src/components/dashboard/index.tsx"
      provides: "Mobile-responsive athlete dashboard with stacked layout"
      contains: "base.*sm.*md"
    - path: "frontend/src/components/dashboard/AdminDashboard.tsx"
      provides: "Mobile-responsive coach dashboard"
      contains: "base.*sm.*md"
    - path: "frontend/src/components/performance/index.tsx"
      provides: "Mobile-responsive performance charts"
      contains: "base.*sm.*md"
  key_links:
    - from: "frontend/src/styles/theme.ts"
      to: "all components"
      via: "Mantine theme responsive props"
      pattern: "base.*sm.*md"
---

<objective>
Audit and fix mobile responsiveness across ALL existing screens. Every primary view must be usable on a mobile phone (375px viewport minimum).

Purpose: Athletes use the app at the gym on their phones. Coaches may check on athletes from their phone. Every screen -- not just the new active workout flow -- must work on mobile. This plan systematically audits and fixes all existing screens.

Output: All primary screens mobile-responsive with proper touch targets, reflowed layouts, and readable text on small viewports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-deploy/07-05-SUMMARY.md

@DESIGN.md
@frontend/src/styles/theme.ts
@frontend/src/components/dashboard/index.tsx
@frontend/src/components/dashboard/AdminDashboard.tsx
@frontend/src/components/layout/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix coach + athlete dashboards and training screens</name>
  <files>
    frontend/src/components/dashboard/index.tsx
    frontend/src/components/dashboard/AdminDashboard.tsx
    frontend/src/components/dashboard/AthleteStats.tsx
    frontend/src/components/dashboard/RecoveryStatus.tsx
    frontend/src/components/dashboard/TrainingSummary.tsx
    frontend/src/components/dashboard/UpcomingWorkouts.tsx
    frontend/src/components/training/TrainingHeader.tsx
    frontend/src/components/training/TrainingRecommendations.tsx
    frontend/src/components/training/LoadAdjustments.tsx
    frontend/src/components/training/WorkoutCalendar.tsx
  </files>
  <action>
    Read each component file. For each, apply these mobile responsiveness fixes:

    1. **Grid layouts**: Replace fixed-column grids with responsive Mantine Grid cols:
       - `<Grid.Col span={{ base: 12, sm: 6, md: 4 }}>` for card grids
       - Single column on mobile (base: 12), multi-column on tablet/desktop

    2. **Touch targets**: Ensure all buttons, links, and interactive elements are at least 44px tall:
       - Buttons: `size="md"` minimum on mobile, use `size={{ base: 'md', md: 'sm' }}`
       - ActionIcons: `size={{ base: 40, md: 32 }}`
       - Tab buttons: ensure tab labels have enough padding

    3. **Typography**: Use responsive font sizes:
       - Headings: `fz={{ base: 'lg', md: 'xl' }}` or similar
       - Body text: minimum 14px on mobile

    4. **Spacing**: Use responsive padding/margin:
       - Cards: `p={{ base: 'sm', md: 'lg' }}`
       - Page containers: `px={{ base: 'xs', md: 'lg' }}`

    5. **Dashboard-specific fixes**:
       - Athlete dashboard: stack cards vertically on mobile
       - Coach dashboard (AdminDashboard): athlete roster table should scroll horizontally or switch to card layout on mobile
       - Stats cards: 1 column on mobile, 2-3 on desktop
       - Recovery status: simplify layout on mobile (hide less critical info)

    6. **Training screens**:
       - WorkoutCalendar: on mobile, show list view instead of calendar grid
       - TrainingRecommendations: stack recommendation cards vertically
       - LoadAdjustments: ensure charts/data display fits narrow viewport

    Use Mantine responsive props throughout (base/sm/md/lg pattern, NOT media queries or CSS).
  </action>
  <verify>
    Run: `cd frontend && pnpm build` -- should succeed
    Run: `grep -c "base:" frontend/src/components/dashboard/index.tsx` -- should find responsive prop usage
    Run: `grep -c "base:" frontend/src/components/dashboard/AdminDashboard.tsx` -- should find responsive prop usage
  </verify>
  <done>
    - Athlete dashboard stacks cards vertically on mobile
    - Coach dashboard table scrolls or converts to cards on mobile
    - Training screens reflow to single column on mobile
    - All touch targets are 44px+ on mobile
    - Typography is readable on 375px viewport
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix wellbeing, performance, check-in, settings, and profile screens</name>
  <files>
    frontend/src/components/wellbeing/index.tsx
    frontend/src/components/wellbeing/WellbeingCalendar.tsx
    frontend/src/components/check-in/index.tsx
    frontend/src/components/performance/index.tsx
    frontend/src/components/charts/TrendCharts.tsx
    frontend/src/components/settings/Settings.tsx
    frontend/src/components/feedback/DailyFeedbackForm.tsx
    frontend/src/app/profile/page.tsx
  </files>
  <action>
    Read each component file. Apply the same mobile responsiveness pattern:

    1. **Wellbeing screens**:
       - WellbeingCalendar: on mobile, switch to list/timeline view or horizontal scroll
       - Wellbeing trends: ensure charts have minimum height and are touch-scrollable
       - Check-in form: full-width inputs, slider controls large enough for thumb use

    2. **Performance screens**:
       - Performance charts (TrendCharts): set `minHeight` so charts don't collapse, add horizontal scroll for dense data
       - Chart container: `w="100%"` with `style={{ overflowX: 'auto' }}`
       - Metric selector: use horizontal ScrollArea or Select dropdown on mobile (not inline buttons if they wrap poorly)

    3. **Check-in form**:
       - Already designed for quick use (per Phase 4) but verify input sizes
       - Slider controls: ensure thumb is large (Mantine `thumbSize` prop)
       - Submit button: full width on mobile `fullWidth={{ base: true, md: false }}` (Mantine doesn't support this -- use className or simply `fullWidth`)

    4. **Settings page**:
       - Form fields full width on mobile
       - Section headings readable
       - Save buttons accessible without excessive scrolling

    5. **Profile page**:
       - Stack layout vertically on mobile
       - Avatar/info section responsive

    6. **DailyFeedbackForm**:
       - Ensure RPE/mood selectors have large touch targets
       - Text areas full width

    Apply DESIGN.md spacing and sizing tokens throughout.
  </action>
  <verify>
    Run: `cd frontend && pnpm build` -- should succeed
    Run: `grep -c "base:" frontend/src/components/performance/index.tsx` -- should find responsive props
    Run: `grep -c "base:" frontend/src/components/check-in/index.tsx` -- should find responsive props
  </verify>
  <done>
    - Wellbeing screens render correctly on mobile with touch-friendly controls
    - Performance charts don't collapse on narrow viewports
    - Check-in form is thumb-friendly with large sliders
    - Settings and profile pages stack vertically on mobile
    - All screens pass visual inspection at 375px width
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile responsiveness fixes across all primary screens</what-built>
  <how-to-verify>
    1. Open the app at http://localhost:3000 in Chrome DevTools
    2. Toggle device toolbar (Ctrl+Shift+M) and select iPhone SE (375px)
    3. Navigate through each screen and verify:
       - Athlete Dashboard: cards stack vertically, "Start Workout" button is prominent and full-width
       - Coach Dashboard: athlete roster is readable (scrolls horizontally or cards), tabs work
       - Training History: workout list is readable, filters accessible
       - Wellbeing Check-in: sliders large enough, submit button full-width
       - Wellbeing Trends: charts visible with minimum height
       - Performance Charts: charts visible, metric selector works
       - Settings: form fields full-width, readable
       - Profile: stacked layout, readable
    4. Verify all buttons/links have at least 44px touch target (no tiny click areas)
    5. Verify no horizontal overflow causing page-level scrollbar (individual components may scroll)
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific screens that need further fixes</resume-signal>
</task>

</tasks>

<verification>
- All primary screens render correctly at 375px viewport width
- No page-level horizontal overflow on any screen
- Touch targets are 44px+ on all interactive elements
- Charts have minimum height and don't collapse
- Forms have full-width inputs on mobile
- `pnpm build` succeeds
</verification>

<success_criteria>
- Every screen usable on iPhone SE (375px) viewport
- Coach and athlete dashboards have appropriate mobile layouts
- Performance charts readable on mobile (with scroll if needed)
- Check-in form completable with thumb-only interaction
- No regressions on desktop layout
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-09-SUMMARY.md`
</output>
