---
phase: 07-polish-deploy
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - docker-compose.yml
  - backend/sql/init/01-schema.sql
  - backend/sql/init/02-seed-users.sql
  - backend/sql/init/03-seed-data.sql
  - backend/sql/init/04-seed-exercises.sql
  - backend/Dockerfile
  - frontend/Dockerfile
  - .env.example
autonomous: true

must_haves:
  truths:
    - "docker-compose up starts PostgreSQL + backend + frontend in one command"
    - "Database is seeded with schema, dev users, Joshua Hudson data, and exercise library on first run"
    - "Backend connects to local PostgreSQL and serves API requests"
    - "Frontend connects to backend API and renders pages"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full stack dev environment with db, backend, frontend services"
      contains: "postgres:16-alpine"
    - path: "backend/sql/init/01-schema.sql"
      provides: "Consolidated schema with all tables (sports, exercises, athletes, workouts, coaches, weekly_plans, etc.)"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "backend/sql/init/02-seed-users.sql"
      provides: "Dev users (coach + athlete) and public.users table"
      contains: "coach@dev.local"
    - path: "backend/sql/init/03-seed-data.sql"
      provides: "Joshua Hudson training data (workouts, wellbeing, performance)"
      contains: "INSERT INTO"
    - path: "backend/sql/init/04-seed-exercises.sql"
      provides: "Full exercise library"
      contains: "INSERT INTO"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/sql/init/"
      via: "volume mount to docker-entrypoint-initdb.d"
      pattern: "docker-entrypoint-initdb.d"
    - from: "docker-compose.yml"
      to: "backend/app/core/config.py"
      via: "environment variables for AUTH_PROVIDER=dev, DATABASE_URL"
      pattern: "AUTH_PROVIDER.*dev"
    - from: "backend/sql/init/02-seed-users.sql"
      to: "backend/app/core/config.py"
      via: "DEV_USER_ID matches seed user UUID"
      pattern: "00000000-0000-0000-0000-000000000001"
---

<objective>
Build a complete Docker Compose development environment with PostgreSQL, seeded data, and zero external dependencies.

Purpose: Developers must be able to run `docker-compose up` and get a fully working application with real data. No Supabase credentials needed. The consolidated schema includes all tables from all phases, the seed data includes Joshua Hudson's training data, and the dev auth bypass handles authentication.

Output: Updated docker-compose.yml with PostgreSQL + backend + frontend. SQL init scripts for schema + seed data. Updated Dockerfiles. Environment variable documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-deploy/07-RESEARCH.md
@.planning/phases/07-polish-deploy/07-01-SUMMARY.md

@docker-compose.yml
@backend/sql/fresh_clean_schema.sql
@backend/sql/production_schema.sql
@backend/sql/weekly_plans_migration.sql
@backend/Dockerfile
@frontend/Dockerfile
@backend/converted_data/athletes.json
@backend/converted_data/workouts.json
@backend/converted_data/exercises.json
@backend/converted_data/wellbeing_entries.json
@backend/converted_data/performance_tests.json
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidated schema and seed SQL scripts</name>
  <files>
    backend/sql/init/01-schema.sql
    backend/sql/init/02-seed-users.sql
    backend/sql/init/03-seed-data.sql
    backend/sql/init/04-seed-exercises.sql
  </files>
  <action>
    1. Create `backend/sql/init/` directory.

    2. Create `backend/sql/init/01-schema.sql` -- consolidated, idempotent schema:
       - Start with `CREATE EXTENSION IF NOT EXISTS "uuid-ossp";`
       - Create a `public.users` table (id UUID PK, email TEXT UNIQUE, role TEXT, created_at) -- replaces auth.users for local dev
       - Include ALL tables from fresh_clean_schema.sql: sports, exercises, athletes, workouts, workout_exercises, performance_metrics, training_loads, wellbeing_assessments, training_recommendations
       - Include coaches table from production_schema.sql (remove `REFERENCES auth.users(id)` FK, keep user_id as plain UUID)
       - Include coach_athletes table from production_schema.sql
       - Include weekly_plans table from weekly_plans_migration.sql (keep coaches FK since coaches table is in same script)
       - Use `CREATE TABLE IF NOT EXISTS` for all tables
       - Remove ALL `REFERENCES auth.users(id)` constraints -- local Postgres has no auth schema
       - Remove ALL RLS policies and `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` -- not needed for local dev (backend uses direct connection)
       - Keep all indexes with `CREATE INDEX IF NOT EXISTS`
       - Include the `update_updated_at_column()` trigger function and apply to relevant tables
       - Keep all CHECK constraints (workout rpe, intensity_level, etc.)
       - Modify athletes table: `user_id uuid` without FK to auth.users
       - At end: `SELECT 'Schema initialized successfully' as status;`

    3. Create `backend/sql/init/02-seed-users.sql`:
       - Insert dev users into public.users table
       - Coach: id='00000000-0000-0000-0000-000000000001', email='coach@dev.local', role='coach'
       - Athlete (Joshua Hudson): id='00000000-0000-0000-0000-000000000002', email='josh@dev.local', role='athlete'
       - Insert into coaches table: id='00000000-0000-0000-0000-000000000010', user_id references coach user
       - Insert into athletes table: id='00000000-0000-0000-0000-000000000020', user_id references athlete user, first_name='Joshua', last_name='Hudson', email='josh@dev.local', sport_id=(SELECT id FROM sports WHERE name='Bobsleigh')
       - Insert coach-athlete relationship into coach_athletes
       - All INSERTs use `ON CONFLICT DO NOTHING` for idempotency
       - NOTE: The athletes row must be created AFTER sports is populated (01-schema.sql includes the Bobsleigh sport seed)

    4. Create `backend/sql/init/03-seed-data.sql`:
       - Read the JSON files in `backend/converted_data/` to understand the data structure
       - Generate INSERT statements for Joshua Hudson's data:
         - Workouts from workouts.json (link to athlete_id='00000000-0000-0000-0000-000000000020')
         - Wellbeing entries from wellbeing_entries.json
         - Performance tests from performance_tests.json
       - Use `ON CONFLICT DO NOTHING` for idempotency
       - If the JSON files are large, generate a representative subset (at least 30 workouts, 30 wellbeing entries, 10 performance tests) to keep the SQL manageable
       - Include workout_exercises if the workouts.json contains exercise detail data

    5. Create `backend/sql/init/04-seed-exercises.sql`:
       - Read exercises.json from converted_data
       - Generate INSERT statements for the full exercise library
       - Link exercises to Bobsleigh sport_id
       - Use `ON CONFLICT (name) DO NOTHING` for idempotency
       - Include any Cyrus Gray methodology exercises if they appear in the data

    All SQL scripts must be idempotent (safe to run multiple times).
    All SQL scripts must use explicit UUIDs for cross-referenced records (don't rely on uuid_generate_v4() for seed data since IDs need to be deterministic).
  </action>
  <verify>
    Run: `cat backend/sql/init/01-schema.sql | head -5` -- should start with CREATE EXTENSION
    Run: `grep -c "CREATE TABLE IF NOT EXISTS" backend/sql/init/01-schema.sql` -- should be 12+ tables
    Run: `grep "auth.users" backend/sql/init/01-schema.sql` -- should return nothing (no auth.users references)
    Run: `grep "ON CONFLICT" backend/sql/init/02-seed-users.sql` -- should find ON CONFLICT clauses
  </verify>
  <done>
    - Consolidated schema includes all tables from all phases
    - No auth.users FK constraints (works with local PostgreSQL)
    - No RLS policies (not needed for direct backend connection)
    - Dev users match the auth provider config UUIDs
    - Joshua Hudson training data is seeded for immediate testing
    - All scripts are idempotent
  </done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose and environment setup</name>
  <files>
    docker-compose.yml
    backend/Dockerfile
    frontend/Dockerfile
    .env.example
  </files>
  <action>
    1. Rewrite `docker-compose.yml`:
       ```yaml
       services:
         db:
           image: postgres:16-alpine
           ports:
             - "5432:5432"
           environment:
             POSTGRES_USER: postgres
             POSTGRES_PASSWORD: postgres
             POSTGRES_DB: bobsleigh
           volumes:
             - postgres_data:/var/lib/postgresql/data
             - ./backend/sql/init:/docker-entrypoint-initdb.d
           healthcheck:
             test: ["CMD-SHELL", "pg_isready -U postgres"]
             interval: 5s
             timeout: 5s
             retries: 5

         backend:
           build:
             context: ./backend
             dockerfile: Dockerfile
           ports:
             - "8000:8000"
           volumes:
             - ./backend:/app
           environment:
             ENVIRONMENT: development
             DATABASE_URL: postgresql://postgres:postgres@db:5432/bobsleigh
             AUTH_PROVIDER: dev
             DEV_USER_ID: "00000000-0000-0000-0000-000000000001"
             DEV_USER_EMAIL: "coach@dev.local"
             DEV_USER_ROLE: "coach"
             CORS_ORIGINS: "http://localhost:3000"
             SECRET_KEY: "dev-secret-key-not-for-production"
           depends_on:
             db:
               condition: service_healthy
           command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

         frontend:
           build:
             context: ./frontend
             dockerfile: Dockerfile
           ports:
             - "3000:3000"
           environment:
             NEXT_PUBLIC_API_URL: http://localhost:8000
             NEXT_PUBLIC_AUTH_MODE: dev
           depends_on:
             - backend

       volumes:
         postgres_data:
       ```
       Key changes from current docker-compose.yml:
       - Add db service with PostgreSQL (was missing healthcheck and init volume)
       - Remove all SUPABASE_* environment variables from backend and frontend
       - Add AUTH_PROVIDER=dev and DEV_USER_* settings
       - Add NEXT_PUBLIC_AUTH_MODE=dev for frontend
       - Add healthcheck on db and `condition: service_healthy` on backend
       - Remove frontend volume mount (preserve standalone build output per existing decision)
       - Mount ./backend/sql/init to /docker-entrypoint-initdb.d for auto-seeding

    2. Update `backend/Dockerfile`:
       - Read existing Dockerfile first
       - Ensure it installs all requirements
       - Ensure WORKDIR is /app
       - Default CMD should be `uvicorn app.main:app --host 0.0.0.0 --port 8000`
       - The docker-compose command override adds --reload for development

    3. Update `frontend/Dockerfile`:
       - Read existing Dockerfile first
       - Ensure it uses pnpm (not npm) per project conventions
       - Build stage should use `pnpm build`
       - Runtime stage should use `pnpm start` or `node server.js` for standalone
       - For dev: docker-compose could override with `pnpm dev` but standalone build is fine for testing

    4. Update `.env.example`:
       - Document all environment variables with descriptions
       - Include dev defaults that work out-of-the-box with docker-compose
       - Include commented-out Supabase/production settings
       - Group by: Database, Auth, Frontend, Backend
  </action>
  <verify>
    Run: `docker compose config` -- validates docker-compose.yml syntax (from project root)
    Run: `cat .env.example` -- should show all required variables with descriptions
  </verify>
  <done>
    - docker-compose.yml starts PostgreSQL + backend + frontend with zero external deps
    - Database auto-seeds schema + data on first run via init scripts
    - Backend uses dev auth provider (no Supabase needed)
    - Frontend uses dev auth mode (no Supabase needed)
    - .env.example documents all environment variables
  </done>
</task>

</tasks>

<verification>
- `docker compose config` validates without errors
- SQL init scripts are idempotent (CREATE IF NOT EXISTS, ON CONFLICT DO NOTHING)
- No reference to auth.users or Supabase-specific schemas in init SQL
- DEV_USER_ID in docker-compose matches seed user UUID in 02-seed-users.sql
- Backend Dockerfile builds successfully
- Frontend Dockerfile builds successfully
</verification>

<success_criteria>
- `docker-compose up` starts all services and the database is seeded with data
- Backend API is accessible at http://localhost:8000/docs
- Frontend is accessible at http://localhost:3000
- Health check at http://localhost:8000/health returns `database: connected`
- No external credentials required for local development
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-04-SUMMARY.md`
</output>
