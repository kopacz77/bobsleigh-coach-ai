---
phase: 04-wellness-recovery
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/hooks/useWellbeing.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/check-in/DailyCheckIn.tsx
  - frontend/src/app/wellbeing/page.tsx
autonomous: true

must_haves:
  truths:
    - "Athlete can complete daily wellness check-in in under 60 seconds"
    - "Check-in form submits through backend API (not direct Supabase)"
    - "Athlete can explicitly flag an injury concern via a dedicated checkbox (not just writing notes)"
    - "Submitting check-in shows success notification and updates readiness display"
    - "Re-opening check-in on same day pre-fills previously submitted values"
  artifacts:
    - path: "frontend/src/hooks/useWellbeing.ts"
      provides: "React Query hooks for check-in CRUD"
      exports: ["useCheckInToday", "useSubmitCheckIn", "useWellbeingHistory"]
    - path: "frontend/src/lib/api.ts"
      provides: "wellbeingAPI client methods"
      contains: "wellbeingAPI"
    - path: "frontend/src/components/check-in/DailyCheckIn.tsx"
      provides: "Consolidated daily check-in form"
      min_lines: 50
  key_links:
    - from: "frontend/src/hooks/useWellbeing.ts"
      to: "/api/wellbeing/checkin"
      via: "wellbeingAPI.submitCheckIn"
      pattern: "wellbeing.*checkin"
    - from: "frontend/src/components/check-in/DailyCheckIn.tsx"
      to: "frontend/src/hooks/useWellbeing.ts"
      via: "useSubmitCheckIn mutation"
      pattern: "useSubmitCheckIn"
    - from: "frontend/src/app/wellbeing/page.tsx"
      to: "frontend/src/hooks/useWellbeing.ts"
      via: "useCheckInToday for readiness display"
      pattern: "useCheckInToday"
---

<objective>
Consolidate the daily check-in form and wire it to the new backend API so athletes can complete wellness check-ins quickly and reliably.

Purpose: The existing frontend has two competing check-in forms (DailyCheckIn and WellbeingAssessment), both talking directly to Supabase with inconsistent table names. This plan consolidates into a single streamlined form that goes through the backend API, calculates readiness server-side, and takes under 60 seconds to complete. This satisfies WELL-01 (fast check-in) and WELL-03 (injury flagging).

Output: Working daily check-in form wired to backend API, React Query hooks for wellbeing data, updated wellbeing page
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wellness-recovery/04-01-SUMMARY.md

Key existing patterns to follow:
- frontend/src/hooks/useTraining.ts — React Query hook pattern (useQuery + useMutation)
- frontend/src/lib/api.ts — API client pattern (wellbeingAPI object with methods)
- frontend/src/components/training/WorkoutForm.tsx — form submission + notifications pattern
- frontend/src/components/check-in/DailyCheckIn.tsx — existing check-in UI (to consolidate)

Key decisions:
- Use useSupabase() hook with null guards for Supabase operations
- But for backend API calls, use the axios api client from @/lib/api.ts (not Supabase directly)
- Use Mantine notifications.show() for form feedback
- Use React Query for server state
- Injury flagging uses explicit flag_concern boolean checkbox + [CONCERN] prefix in notes (see 04-01 backend contract)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wellbeing API client methods and React Query hooks</name>
  <files>
    frontend/src/lib/api.ts
    frontend/src/hooks/useWellbeing.ts
  </files>
  <action>
    1. Add wellbeingAPI to frontend/src/lib/api.ts (following exact pattern of trainingAPI):
       ```typescript
       export const wellbeingAPI = {
         submitCheckIn: (data: { sleep_quality: number; stress_level: number; nutrition_quality: number; physical_readiness: number; mental_clarity: number; notes?: string; flag_concern?: boolean }) =>
           api.post("/api/wellbeing/checkin", data),
         getCheckInToday: () =>
           api.get("/api/wellbeing/checkin/today"),
         getHistory: (days = 30) =>
           api.get(`/api/wellbeing/history?days=${days}`),
       };
       ```

    2. Create frontend/src/hooks/useWellbeing.ts with React Query hooks:
       - useCheckInToday(): useQuery wrapping wellbeingAPI.getCheckInToday(), queryKey: ["wellbeing", "checkin", "today"], staleTime: 5min. Handle 204 response (return null).
       - useSubmitCheckIn(): useMutation wrapping wellbeingAPI.submitCheckIn(), onSuccess: invalidate ["wellbeing"] queries, show success notification. onError: show error notification.
       - useWellbeingHistory(days: number): useQuery wrapping wellbeingAPI.getHistory(days), queryKey: ["wellbeing", "history", days], staleTime: 5min.

    Follow the exact hook pattern from useTraining.ts — return the React Query result directly.
  </action>
  <verify>
    cd /home/kopacz/projects/bobsleigh-coach-ai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30
  </verify>
  <done>
    - wellbeingAPI object exported from api.ts with 3 methods (including flag_concern in submitCheckIn)
    - useCheckInToday, useSubmitCheckIn, useWellbeingHistory hooks exported
    - TypeScript compiles without errors in these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild DailyCheckIn form for speed and wire to API</name>
  <files>
    frontend/src/components/check-in/DailyCheckIn.tsx
    frontend/src/app/wellbeing/page.tsx
  </files>
  <action>
    1. Rewrite frontend/src/components/check-in/DailyCheckIn.tsx:
       - Remove ALL direct Supabase imports/usage. Use useSubmitCheckIn and useCheckInToday hooks instead.
       - Remove the userId prop — get user from useAuth() hook or let the backend handle identity.
       - Design for SPEED (under 60 seconds):
         * 5 sliders: sleep quality, stress level, nutrition quality, physical readiness, mental clarity (all 1-10, labeled with low/mid/high marks)
         * Use a compact 2-column SimpleGrid layout (not 4 separate Paper cards)
         * One Mantine Checkbox labeled "Flag injury or concern for coach" — when checked, the submission will include flag_concern: true in the API payload. This is the explicit injury flagging mechanism (avoids false positives from general notes).
         * One optional notes Textarea below the checkbox (placeholder: "Additional notes (optional)"). Notes are submitted as-is; the backend handles prepending "[CONCERN]" when flag_concern is true.
         * One "Submit Check-In" button
         * Show readiness score circle at the top (calculated from current slider values, matching backend formula: round((sleep + (10-stress) + nutrition + physical + mental) / 5))
         * Traffic light color on the readiness circle: green >= 8, yellow >= 5, red < 5
       - On mount: call useCheckInToday() to pre-fill if already submitted today. If data exists, show "Update Check-In" button text instead of "Submit". Pre-fill the flag_concern checkbox based on the has_concern field from the API response. If has_concern is true, also strip the "[CONCERN] " prefix from notes before displaying in the textarea.
       - On submit: call submitCheckIn mutation with {sleep_quality, stress_level, nutrition_quality, physical_readiness, mental_clarity, notes, flag_concern}. Show Mantine notification on success/error.
       - Remove the date picker (always submits for today — server sets date)
       - Remove sleep hours, hydration, nutrition_quality (as separate NumberInputs), training_readiness Select — these are OVER-ENGINEERED for a 60-second check-in. Keep only the 5 core sliders that match the database columns.

    2. Update frontend/src/app/wellbeing/page.tsx:
       - Remove the hardcoded currentUserId = "123e4567..." — no longer needed since backend handles identity
       - Default tab should be "checkin" (not "assessment")
       - Replace the "Daily Assessment" tab content with the rebuilt DailyCheckIn component (no props needed)
       - Remove the WellbeingAssessment tab (it's being replaced by DailyCheckIn)
       - Keep Calendar, Trends, Recovery, and Reflections tabs for now (they can be fixed in Plan 03 or later)
       - Remove userId prop from remaining tab components where possible (they'll be updated later)
  </action>
  <verify>
    cd /home/kopacz/projects/bobsleigh-coach-ai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30
  </verify>
  <done>
    - DailyCheckIn has exactly 5 sliders + 1 concern checkbox + 1 notes field + 1 submit button (completable in under 60 seconds)
    - Form submits through backend API via useSubmitCheckIn mutation (not direct Supabase)
    - Pre-fills from today's existing check-in via useCheckInToday (including concern checkbox state)
    - Readiness traffic light circle displayed at top
    - Explicit "Flag injury or concern" checkbox prevents false positives from general notes
    - Wellbeing page uses DailyCheckIn as default tab with no hardcoded userId
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: cd frontend && npx tsc --noEmit
- DailyCheckIn component renders without errors (no hardcoded userId, no direct Supabase imports)
- wellbeingAPI has submitCheckIn (with flag_concern param), getCheckInToday, getHistory methods
- useSubmitCheckIn invalidates wellbeing queries on success
- DailyCheckIn includes a Checkbox for flagging concerns (grep for "flag_concern" or "Flag injury")
</verification>

<success_criteria>
- Daily check-in form has exactly 5 sliders + 1 concern checkbox + 1 notes field (achievable in under 60 seconds)
- Form data flows: DailyCheckIn -> useSubmitCheckIn -> wellbeingAPI.submitCheckIn -> POST /api/wellbeing/checkin
- Readiness score displayed as traffic light (green/yellow/red)
- Injury flagging uses explicit checkbox (flag_concern: true), not implicit non-empty notes
- No direct Supabase calls from check-in flow
</success_criteria>

<output>
After completion, create `.planning/phases/04-wellness-recovery/04-02-SUMMARY.md`
</output>
