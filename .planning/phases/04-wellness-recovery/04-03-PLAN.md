---
phase: 04-wellness-recovery
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/components/dashboard/CoachReadiness.tsx
  - frontend/src/components/dashboard/CoachDashboard.tsx
  - frontend/src/components/wellbeing/WellbeingTrends.tsx
  - frontend/src/hooks/useWellbeing.ts
  - frontend/src/lib/api.ts
autonomous: true

must_haves:
  truths:
    - "Coach sees readiness traffic light (green/yellow/red) for each athlete"
    - "Coach can see which athletes have explicitly flagged injuries/concerns"
    - "Athlete can view their wellbeing trends over time with real data"
    - "Athletes without today's check-in show as gray (no data)"
  artifacts:
    - path: "frontend/src/components/dashboard/CoachReadiness.tsx"
      provides: "Readiness overview table with traffic lights"
      min_lines: 40
    - path: "frontend/src/components/wellbeing/WellbeingTrends.tsx"
      provides: "Wellbeing trends chart wired to backend API"
      contains: "useWellbeingHistory"
  key_links:
    - from: "frontend/src/components/dashboard/CoachReadiness.tsx"
      to: "/api/wellbeing/coach/readiness"
      via: "wellbeingAPI.getCoachReadiness"
      pattern: "coach.*readiness"
    - from: "frontend/src/components/wellbeing/WellbeingTrends.tsx"
      to: "frontend/src/hooks/useWellbeing.ts"
      via: "useWellbeingHistory hook"
      pattern: "useWellbeingHistory"
    - from: "frontend/src/components/dashboard/CoachDashboard.tsx"
      to: "frontend/src/components/dashboard/CoachReadiness.tsx"
      via: "component import"
      pattern: "CoachReadiness"
---

<objective>
Create coach readiness view and wire athlete wellbeing trends to the backend API.

Purpose: Coaches need to see at a glance which athletes are ready to train (green), need caution (yellow), should rest (red), or haven't checked in (gray). Athletes need to see their own wellbeing trends over time using real data from the backend API. This satisfies WELL-02 (readiness traffic light) and WELL-04 (wellbeing trends).

Output: CoachReadiness component with traffic light table, WellbeingTrends wired to backend API, coach dashboard integration
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wellness-recovery/04-01-SUMMARY.md
@.planning/phases/04-wellness-recovery/04-02-SUMMARY.md

Key existing patterns:
- frontend/src/components/dashboard/CoachDashboard.tsx — coach dashboard structure, athlete list pattern
- frontend/src/components/wellbeing/WellbeingTrends.tsx — existing trends chart (uses recharts, currently direct Supabase)
- frontend/src/hooks/useWellbeing.ts — hooks created in Plan 02 (useCheckInToday, useSubmitCheckIn, useWellbeingHistory already exist)
- frontend/src/lib/api.ts — API client pattern (wellbeingAPI already exists from Plan 02 with submitCheckIn, getCheckInToday, getHistory)

Key decisions:
- Coach role check is in app_metadata (backend handles authorization)
- Traffic light colors: green (>=8), yellow (>=5), red (<5), gray (no data)
- Use Mantine components for all UI
- Use recharts for trend charts (standardized in Phase 1)
- Injury flag uses [CONCERN] prefix in notes; backend returns has_injury_flag based on this prefix (not any non-empty notes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coach readiness API method and hook, create CoachReadiness component</name>
  <files>
    frontend/src/lib/api.ts
    frontend/src/hooks/useWellbeing.ts
    frontend/src/components/dashboard/CoachReadiness.tsx
  </files>
  <action>
    1. Add getCoachReadiness to the existing wellbeingAPI object in frontend/src/lib/api.ts:
       ```typescript
       getCoachReadiness: () => api.get("/api/wellbeing/coach/readiness"),
       ```
       The wellbeingAPI object already exists from Plan 02 with submitCheckIn, getCheckInToday, and getHistory methods. Append this new method to the same object.

    2. Add useCoachReadiness hook to the existing frontend/src/hooks/useWellbeing.ts file:
       ```typescript
       export function useCoachReadiness() {
         return useQuery({
           queryKey: ["wellbeing", "coach", "readiness"],
           queryFn: () => wellbeingAPI.getCoachReadiness().then(res => res.data),
           staleTime: 2 * 60 * 1000, // 2 min — coaches want fresh data
         });
       }
       ```
       The file already exists from Plan 02 with useCheckInToday, useSubmitCheckIn, and useWellbeingHistory hooks. Add this new hook alongside them.

    3. Create frontend/src/components/dashboard/CoachReadiness.tsx:
       - Import useCoachReadiness hook
       - Render a Mantine Table with columns: Athlete Name, Readiness, Status, Concerns
       - For each athlete row:
         * Readiness column: colored circle (16x16 div with borderRadius 50%) using recovery_status color:
           - "green" -> theme.colors.green[6]
           - "yellow" -> theme.colors.yellow[6]
           - "red" -> theme.colors.red[6]
           - "gray" -> theme.colors.gray[4]
         * Status column: Badge with text "Ready" (green), "Caution" (yellow), "Rest" (red), "No Check-in" (gray)
         * Score column: readiness_score number (or "-" if gray)
         * Concerns column: if has_injury_flag is true, show IconAlertTriangle with Tooltip showing "Injury/concern flagged". Otherwise show "-". Note: has_injury_flag is true only when athlete explicitly checked the concern flag, not for general notes.
       - Loading state: Mantine Loader centered
       - Empty state: "No athletes found" text
       - Title: "Athlete Readiness" with subtitle "Today's wellness status"
       - Wrap in a Mantine Card with border
       - Sort athletes: red first, then yellow, then gray, then green (problems surface to top)
  </action>
  <verify>
    cd /home/kopacz/projects/bobsleigh-coach-ai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30
  </verify>
  <done>
    - CoachReadiness component renders table with traffic light indicators
    - Athletes sorted by severity (red > yellow > gray > green)
    - Injury flag shows alert icon only for explicit concern flags (not general notes)
    - Data flows from backend /api/wellbeing/coach/readiness
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate CoachReadiness into dashboard and wire WellbeingTrends to API</name>
  <files>
    frontend/src/components/dashboard/CoachDashboard.tsx
    frontend/src/components/wellbeing/WellbeingTrends.tsx
  </files>
  <action>
    1. Update frontend/src/components/dashboard/CoachDashboard.tsx:
       - Import CoachReadiness from "@/components/dashboard/CoachReadiness"
       - Add CoachReadiness component prominently near the top of the dashboard (after the header/stats area, before workouts/other sections)
       - This replaces or supplements the existing "recentCheckIns" section that currently queries the non-existent "daily_checkins" table directly from Supabase
       - Remove or comment out the direct Supabase query to "daily_checkins" table (lines ~121-146) since this table doesn't exist and the data now comes through the CoachReadiness component

    2. Rewrite frontend/src/components/wellbeing/WellbeingTrends.tsx:
       - Remove ALL direct supabase imports (currently imports from '@/lib/supabase')
       - Use useWellbeingHistory(days) hook instead of direct Supabase queries (hook already exists from Plan 02)
       - Keep the existing recharts chart structure (LineChart with sleep_quality, stress_level, nutrition_quality, physical_readiness, mental_clarity lines)
       - Keep the time range selector (7/30/90/180/365 days) — pass selected days to useWellbeingHistory
       - Remove the "objective" chart type toggle (body_weight, resting_hr, sleep_hours from daily_metrics table) — this data isn't in our simplified schema. Just show the 5 subjective metrics.
       - Handle loading state from React Query (isLoading)
       - Handle empty state: "No wellbeing data yet. Complete your first daily check-in!"
       - Map API response data to chart format:
         ```typescript
         const chartData = data?.map(d => ({
           date: dayjs(d.date).format('MMM D'),
           sleepQuality: d.sleep_quality,
           stressLevel: d.stress_level,
           nutritionQuality: d.nutrition_quality,
           physicalReadiness: d.physical_readiness,
           mentalClarity: d.mental_clarity,
         }));
         ```
  </action>
  <verify>
    cd /home/kopacz/projects/bobsleigh-coach-ai/frontend && npx tsc --noEmit --pretty 2>&1 | head -30
  </verify>
  <done>
    - CoachDashboard includes CoachReadiness component with athlete traffic lights
    - CoachDashboard no longer queries non-existent "daily_checkins" table directly
    - WellbeingTrends uses useWellbeingHistory hook (not direct Supabase)
    - Trends chart shows 5 wellbeing metrics over configurable time range
    - No direct Supabase imports in WellbeingTrends
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: cd frontend && npx tsc --noEmit
- CoachReadiness component exists and imports correctly in CoachDashboard
- WellbeingTrends has zero imports from '@/lib/supabase' (grep confirmation)
- No references to non-existent "daily_checkins" table remain in coach dashboard
</verification>

<success_criteria>
- Coach dashboard shows readiness traffic lights for each athlete at a glance
- Athletes sorted by severity (red/yellow first for attention)
- Injury flags visually prominent with alert icon (only for explicit concern flags)
- Athlete wellbeing trends chart works with real backend API data
- Both components use React Query hooks (not direct Supabase)
</success_criteria>

<output>
After completion, create `.planning/phases/04-wellness-recovery/04-03-SUMMARY.md`
</output>
