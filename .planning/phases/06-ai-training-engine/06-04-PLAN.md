---
phase: 06-ai-training-engine
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/hooks/usePlans.ts
  - frontend/src/components/plans/CoachPlanQueue.tsx
  - frontend/src/components/plans/PlanReviewCard.tsx
  - frontend/src/components/plans/RejectFeedbackModal.tsx
  - frontend/src/components/plans/GenerateAllButton.tsx
autonomous: true

must_haves:
  truths:
    - "Coach sees a queue of pending plans for their athletes"
    - "Coach can click a plan to see the full 7-day detail"
    - "Coach can approve a plan with one click"
    - "Coach can reject a plan and provide feedback notes"
    - "Coach can generate plans for all athletes with one button"
    - "Injury risk warnings display on plans with moderate/high risk"
  artifacts:
    - path: "frontend/src/hooks/usePlans.ts"
      provides: "React Query hooks for plan CRUD and approval"
      exports: ["usePendingPlans", "useApprovePlan", "useRejectPlan", "useGenerateBatch"]
    - path: "frontend/src/components/plans/CoachPlanQueue.tsx"
      provides: "Coach plan review queue page"
      min_lines: 50
    - path: "frontend/src/components/plans/PlanReviewCard.tsx"
      provides: "Single plan detail with approve/reject"
      min_lines: 80
    - path: "frontend/src/components/plans/RejectFeedbackModal.tsx"
      provides: "Modal for rejection feedback notes"
      min_lines: 30
  key_links:
    - from: "frontend/src/hooks/usePlans.ts"
      to: "/api/plans/*"
      via: "plansAPI calls via axios"
      pattern: "api\\.\\(get\\|post\\)"
    - from: "frontend/src/components/plans/CoachPlanQueue.tsx"
      to: "frontend/src/hooks/usePlans.ts"
      via: "usePendingPlans hook"
      pattern: "usePendingPlans"
    - from: "frontend/src/lib/api.ts"
      to: "/api/plans/*"
      via: "plansAPI export"
      pattern: "plansAPI"
---

<objective>
Build the coach-facing plan review UI: a queue of pending plans, detailed plan review cards, approve/reject workflow with feedback modal, and batch generation button.

Purpose: This is the "coach disposes" half of the AI-proposes/coach-disposes workflow (AI-02). Without this UI, the coach has no way to review, approve, or reject AI-generated plans.

Output: React Query hooks for plan operations, coach plan queue component, plan review card with day-by-day detail, rejection feedback modal, and batch generation button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ai-training-engine/06-RESEARCH.md
@.planning/phases/06-ai-training-engine/06-02-SUMMARY.md

@frontend/src/lib/api.ts
@frontend/src/hooks/useCoachDashboard.ts
@frontend/src/hooks/useTraining.ts
@frontend/src/components/dashboard/index.tsx
@frontend/src/components/coach/TrainingApprovalPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plans API client and React Query hooks</name>
  <files>frontend/src/lib/api.ts, frontend/src/hooks/usePlans.ts</files>
  <action>
**In api.ts**, add a new `plansAPI` export object alongside the existing coachAPI, trainingAPI, etc:

```typescript
export const plansAPI = {
  getPending: () => api.get("/api/plans/pending"),
  getPlan: (planId: string) => api.get(`/api/plans/${planId}`),
  getCurrent: () => api.get("/api/plans/current"),
  getToday: () => api.get("/api/plans/today"),
  generate: (athleteId: string, weekStart: string) =>
    api.post("/api/plans/generate", { athlete_id: athleteId, week_start: weekStart }),
  generateBatch: (weekStart: string) =>
    api.post("/api/plans/generate-batch", { week_start: weekStart }),
  approve: (planId: string) => api.post(`/api/plans/${planId}/approve`),
  reject: (planId: string, rejectionNotes: string) =>
    api.post(`/api/plans/${planId}/reject`, { rejection_notes: rejectionNotes }),
};
```

**Create usePlans.ts** with React Query hooks following the patterns in useCoachDashboard.ts and useTraining.ts:

```typescript
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { plansAPI } from "@/lib/api";
```

Hooks to create:
1. `usePendingPlans()` - useQuery for GET /pending, key: ["plans", "pending"]
2. `usePlan(planId: string)` - useQuery for GET /{planId}, key: ["plans", planId], enabled: !!planId
3. `useCurrentPlan()` - useQuery for GET /current, key: ["plans", "current"]
4. `useTodaysWorkout()` - useQuery for GET /today, key: ["plans", "today"], staleTime: 30000 (30s for fresh adaptation)
5. `useApprovePlan()` - useMutation calling plansAPI.approve, onSuccess: invalidate ["plans", "pending"]
6. `useRejectPlan()` - useMutation calling plansAPI.reject with {planId, rejectionNotes}, onSuccess: invalidate ["plans", "pending"]
7. `useGenerateBatch()` - useMutation calling plansAPI.generateBatch, onSuccess: invalidate ["plans", "pending"]

For queries that may return 204 (getCurrent, getToday): handle the empty response (axios returns empty data on 204). Return null for these cases.

Each hook should follow the existing pattern: export named function, return the useQuery/useMutation result.
  </action>
  <verify>
1. `grep "plansAPI" frontend/src/lib/api.ts` should show the export
2. `grep -c "export function" frontend/src/hooks/usePlans.ts` should return 7
  </verify>
  <done>plansAPI added to api.ts with all plan endpoints. usePlans.ts exports 7 hooks for pending plans, plan detail, current plan, today's workout, approve, reject, and batch generate.</done>
</task>

<task type="auto">
  <name>Task 2: Build coach plan review UI components</name>
  <files>frontend/src/components/plans/CoachPlanQueue.tsx, frontend/src/components/plans/PlanReviewCard.tsx, frontend/src/components/plans/RejectFeedbackModal.tsx, frontend/src/components/plans/GenerateAllButton.tsx</files>
  <action>
Create 4 components in a new `frontend/src/components/plans/` directory. Use Mantine UI v7 components throughout (matching existing codebase: Card, Button, Badge, Modal, Textarea, Group, Stack, Text, Title, Paper, Alert, Loader, Tabs, Table, etc.). Use Mantine prop conventions: justify, gap, ta, c, fs.

**CoachPlanQueue.tsx** - Main queue page for pending plans
- Uses usePendingPlans() hook
- Shows loading state (Loader) and empty state ("No plans pending review")
- Lists pending plans as a stack of PlanReviewCard components
- Shows GenerateAllButton at the top
- Each plan card shows: athlete name, week dates, training phase, injury risk badge, version number
- Injury risk badge colors: green="low", yellow="moderate", red="high"

**PlanReviewCard.tsx** - Expandable card showing full plan detail
- Props: plan (the plan object from API)
- Shows plan summary: athlete name, week_start - week_end, training_phase, injury_risk_score
- If injury_risk_score >= 0.3 (moderate+): show Alert component with risk factors list
- Expandable section showing day-by-day breakdown:
  - For each day in plan_data.days: show day_name, session_type, title
  - For each section (warm_up, main, accessory, cool_down): show exercises with sets/reps/weight
  - Use a simple Table or stacked layout
- Two action buttons at bottom:
  - "Approve" (green Button) -> calls useApprovePlan mutation
  - "Reject" (red Button, variant="outline") -> opens RejectFeedbackModal
- Show success notification (Mantine notifications.show) on approve/reject
- Disable buttons while mutation is pending (loading prop)

**RejectFeedbackModal.tsx** - Modal for entering rejection feedback
- Props: opened (boolean), onClose, onSubmit(notes: string)
- Mantine Modal with title "Reject Plan - Feedback"
- Textarea for rejection_notes (required, minLength: 10)
- Help text: "Describe what should change (e.g., 'too much volume', 'need more sprint work', 'reduce intensity'). This feedback helps the AI generate better plans."
- Submit button calls onSubmit with the notes text
- Validate that notes are non-empty before submit

**GenerateAllButton.tsx** - Batch generation trigger
- Uses useGenerateBatch() mutation
- Calculates next Monday date (or current Monday if today is Monday)
- Renders Button "Generate All Plans" with loading state
- On success: show notification "Plans are being generated. Refresh in a moment."
- On error: show error notification
  </action>
  <verify>
1. `ls frontend/src/components/plans/` should show 4 .tsx files
2. `grep -l "usePendingPlans" frontend/src/components/plans/CoachPlanQueue.tsx` should match
3. `grep -l "useApprovePlan" frontend/src/components/plans/PlanReviewCard.tsx` should match
4. `grep -l "Modal" frontend/src/components/plans/RejectFeedbackModal.tsx` should match
  </verify>
  <done>4 coach plan UI components: CoachPlanQueue shows pending plans list, PlanReviewCard shows day-by-day detail with approve/reject buttons, RejectFeedbackModal captures rejection notes, GenerateAllButton triggers batch generation</done>
</task>

</tasks>

<verification>
- plansAPI in api.ts has all 8 methods (getPending, getPlan, getCurrent, getToday, generate, generateBatch, approve, reject)
- usePlans.ts has 7 React Query hooks
- CoachPlanQueue renders pending plans list using usePendingPlans
- PlanReviewCard shows full plan detail with approve/reject actions
- RejectFeedbackModal validates non-empty rejection notes
- Injury risk displayed as colored badge and alert
- All components use Mantine UI v7 with correct prop naming
</verification>

<success_criteria>
- Coach can see the plan review queue showing all pending plans
- Coach can expand a plan to see the full 7-day breakdown with exercises
- Coach can approve a plan (triggers mutation, invalidates query)
- Coach can reject with feedback (modal enforces non-empty notes)
- High injury risk plans show a warning alert with contributing factors
- Batch generate button creates plans for all athletes
- All hooks invalidate relevant queries on mutation success
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-training-engine/06-04-SUMMARY.md`
</output>
