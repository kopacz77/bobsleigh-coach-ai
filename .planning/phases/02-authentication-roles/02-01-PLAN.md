---
phase: 02-authentication-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/auth/login/page.tsx
  - frontend/src/app/auth/signup/page.tsx
  - frontend/src/lib/supabase.ts
  - frontend/src/hooks/useAuth.ts
  - frontend/src/components/auth/AuthGuard.tsx
  - frontend/src/providers/SupabaseProvider.tsx
autonomous: true

must_haves:
  truths:
    - "User can sign up with email and password via the app"
    - "User can log in with email and password via the app"
    - "User can log in with Google OAuth via the app"
    - "User can log out and is redirected to login page"
    - "Auth session persists across browser refresh (user stays logged in)"
    - "Unauthenticated users are redirected to /auth/login"
    - "Authenticated users on /auth/login are redirected to /dashboard"
  artifacts:
    - path: "frontend/src/app/auth/login/page.tsx"
      provides: "Working login form with email/password and Google OAuth"
      contains: "signInWithPassword"
    - path: "frontend/src/app/auth/signup/page.tsx"
      provides: "Sign-up form with email/password"
      contains: "signUp"
    - path: "frontend/src/hooks/useAuth.ts"
      provides: "Auth hook with user state, login, signup, logout, loading"
      exports: ["useAuth"]
    - path: "frontend/src/components/auth/AuthGuard.tsx"
      provides: "Auth guard that uses onAuthStateChange listener for session restore"
      contains: "onAuthStateChange"
  key_links:
    - from: "frontend/src/app/auth/login/page.tsx"
      to: "supabase.auth.signInWithPassword"
      via: "form submit handler"
      pattern: "signInWithPassword"
    - from: "frontend/src/components/auth/AuthGuard.tsx"
      to: "supabase.auth.onAuthStateChange"
      via: "useEffect listener"
      pattern: "onAuthStateChange"
    - from: "frontend/src/hooks/useAuth.ts"
      to: "SupabaseProvider"
      via: "useSupabase() hook"
      pattern: "useSupabase"
---

<objective>
Wire frontend authentication to real Supabase Auth for email/password signup, email/password login, Google OAuth login, and logout. Fix session persistence so auth survives browser refresh.

Purpose: AUTH-01 (sign up and log in via Supabase Auth) and AUTH-04 (session persists across refresh). These are the foundation for all subsequent auth work.

Output: Working login page, new signup page, and reliable auth state management via Supabase Auth listeners.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files to understand before starting:
@frontend/src/providers/SupabaseProvider.tsx (creates Supabase client with useSupabase() hook)
@frontend/src/lib/supabase.ts (standalone Supabase client + auth helpers)
@frontend/src/hooks/useAuth.ts (current auth hook - uses standalone client, NOT useSupabase())
@frontend/src/components/auth/AuthGuard.tsx (current guard - uses standalone client)
@frontend/src/app/auth/login/page.tsx (current login page - email submit is mocked)
@frontend/src/app/layout.tsx (provider hierarchy: ReactQuery > Supabase > Mantine > AuthGuard)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix auth hook and guard to use SupabaseProvider + add signup/signIn functions</name>
  <files>
    frontend/src/lib/supabase.ts
    frontend/src/hooks/useAuth.ts
    frontend/src/components/auth/AuthGuard.tsx
    frontend/src/providers/SupabaseProvider.tsx
  </files>
  <action>
  There are TWO Supabase clients right now: one in `frontend/src/lib/supabase.ts` (standalone, created at module load) and one in `frontend/src/providers/SupabaseProvider.tsx` (created in useEffect, available via useSupabase()). Phase 1 migrated all components to use useSupabase(), but useAuth.ts and AuthGuard.tsx still import from the standalone `@/lib/supabase.ts`. This dual-client causes session state desync.

  **Fix `frontend/src/lib/supabase.ts`:**
  - Add `signInWithEmail(email, password)` function that calls `supabase.auth.signInWithPassword({ email, password })`
  - Add `signUpWithEmail(email, password)` function that calls `supabase.auth.signUp({ email, password })`
  - Keep existing `signInWithGoogle()`, `signOut()`, `getCurrentUser()` functions
  - Keep database access functions (getAthleteProfile, etc.) as-is -- they will be migrated to use useSupabase in a future phase

  **Rewrite `frontend/src/hooks/useAuth.ts`:**
  - Import `useSupabase` from `@/providers/SupabaseProvider` instead of importing from `@/lib/supabase`
  - Get `supabase` client from `useSupabase()` hook
  - Add null guard: if `!supabase` return early with loading state
  - Use `supabase.auth.onAuthStateChange()` in useEffect to listen for SIGNED_IN, SIGNED_OUT, TOKEN_REFRESHED events
  - Get initial session via `supabase.auth.getSession()` (not getUser -- getSession is synchronous from local storage)
  - Export: `{ user, session, loading, isAuthenticated, login(email, password), loginWithGoogle(), signup(email, password), logout() }`
  - The `login` function should call `supabase.auth.signInWithPassword({ email, password })` and return `{ data, error }`
  - The `loginWithGoogle` function should call `supabase.auth.signInWithOAuth({ provider: 'google' })`
  - The `signup` function should call `supabase.auth.signUp({ email, password })` and return `{ data, error }`
  - The `logout` function should call `supabase.auth.signOut()` and push to '/auth/login'

  **Rewrite `frontend/src/components/auth/AuthGuard.tsx`:**
  - Import `useSupabase` from `@/providers/SupabaseProvider` instead of `getCurrentUser` from `@/lib/supabase`
  - Get `supabase` client from `useSupabase()` hook
  - Use `supabase.auth.onAuthStateChange()` listener in useEffect to detect auth state
  - On SIGNED_OUT or no session: redirect to `/auth/login` (unless already on `/auth/` page)
  - On SIGNED_IN: if on `/auth/` page, redirect to `/dashboard`
  - Otherwise: show children
  - Handle the `loading` state from the provider - show Loader while SupabaseProvider is initializing
  - Clean up subscription on unmount

  **No changes needed to SupabaseProvider.tsx** -- it already creates the client with `persistSession: true` and `autoRefreshToken: true`.
  </action>
  <verify>
  Run `cd /home/kopacz/projects/bobsleigh-coach-ai/frontend && pnpm build` -- must compile without TypeScript errors.
  Verify no imports of standalone supabase client remain in useAuth.ts or AuthGuard.tsx:
  `grep -n "from.*@/lib/supabase" frontend/src/hooks/useAuth.ts frontend/src/components/auth/AuthGuard.tsx` should return nothing.
  Verify useSupabase is used: `grep -n "useSupabase" frontend/src/hooks/useAuth.ts frontend/src/components/auth/AuthGuard.tsx` should show imports.
  </verify>
  <done>
  useAuth hook and AuthGuard both use the SupabaseProvider client (no dual-client desync). useAuth exposes login/signup/logout functions that call real Supabase Auth methods. AuthGuard uses onAuthStateChange for reliable session detection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire login page to real Supabase Auth and create signup page</name>
  <files>
    frontend/src/app/auth/login/page.tsx
    frontend/src/app/auth/signup/page.tsx
  </files>
  <action>
  **Rewrite `frontend/src/app/auth/login/page.tsx`:**
  - Import `useAuth` from `@/hooks/useAuth` (do NOT import from @/lib/supabase directly)
  - Get `{ login, loginWithGoogle, loading: authLoading }` from useAuth()
  - Replace the mock `handleSubmit` with a real one:
    - Call `const { error } = await login(values.email, values.password)`
    - If error, display error.message in the error state
    - If success, router.push('/dashboard') (AuthGuard will also handle this, but explicit redirect gives better UX)
  - Replace `handleGoogleLogin` to use `loginWithGoogle()` from useAuth
  - Add a "Don't have an account? Sign up" link below the form, linking to `/auth/signup`
  - Add a "Forgot password?" link that calls `supabase.auth.resetPasswordForEmail()` -- for now, just show a simple info message "Check your email for a reset link" after calling it
  - Keep the existing Mantine UI structure (Card, Stack, TextInput, PasswordInput, Button, Divider, Google button)
  - Fix the `color="red"` prop on Text to `c="red"` (Mantine v7)

  **Create `frontend/src/app/auth/signup/page.tsx`:**
  - Use the same visual layout as login page (Card, Container, Stack)
  - Form fields: email (TextInput), password (PasswordInput), confirm password (PasswordInput)
  - Validation: email format, password >= 8 chars, passwords match
  - On submit: call `signup(email, password)` from useAuth()
  - On success: show a message "Check your email to confirm your account" (Supabase sends confirmation email by default)
  - On error: display error.message
  - Include "Already have an account? Sign in" link to `/auth/login`
  - Include Google OAuth button (same as login page)
  - Mark with `"use client"` directive at the top
  </action>
  <verify>
  Run `cd /home/kopacz/projects/bobsleigh-coach-ai/frontend && pnpm build` -- must compile without errors.
  Verify signup page exists: `ls frontend/src/app/auth/signup/page.tsx`
  Verify no mock setTimeout in login: `grep -n "setTimeout" frontend/src/app/auth/login/page.tsx` should return nothing.
  Verify signInWithPassword is used: `grep -n "login\|signInWithPassword" frontend/src/app/auth/login/page.tsx` should show the real auth call.
  </verify>
  <done>
  Login page calls real Supabase Auth for email/password and Google OAuth. Signup page exists with email/password registration. No mock auth calls remain. Both pages link to each other for navigation.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` in frontend directory completes without errors
2. No references to mock auth (setTimeout, hardcoded credentials) in login page
3. Signup page exists at frontend/src/app/auth/signup/page.tsx
4. useAuth hook uses useSupabase() from provider (not standalone client)
5. AuthGuard uses onAuthStateChange for reliable session detection
6. `grep -rn "from.*@/lib/supabase" frontend/src/hooks/useAuth.ts frontend/src/components/auth/AuthGuard.tsx` returns nothing
</verification>

<success_criteria>
- Login page submits to Supabase Auth (signInWithPassword for email, signInWithOAuth for Google)
- Signup page submits to Supabase Auth (signUp)
- Auth state managed through single SupabaseProvider client
- onAuthStateChange listener properly restores session on browser refresh
- AuthGuard redirects unauthenticated users to /auth/login
- Frontend compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-roles/02-01-SUMMARY.md`
</output>
