---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/components/wellbeing/Reflection.tsx
  - frontend/src/components/wellbeing/WellbeingAssessment.tsx
  - frontend/src/app/demo/page.tsx
  - frontend/src/utils/api.ts
  - frontend/src/components/dashboard/AthleteDashboard.tsx
  - frontend/src/components/dashboard/CoachDashboard.tsx
  - frontend/src/components/dashboard/AdminDashboard.tsx
  - frontend/src/components/wellbeing/RecoveryHealth.tsx
  - frontend/src/components/wellbeing/PhysicalMetrics.tsx
  - frontend/src/components/wellbeing/MoodTracking.tsx
  - frontend/src/components/wellbeing/WellbeingCalendar.tsx
  - frontend/src/components/wellbeing/WellbeingTrends.tsx
  - frontend/src/components/check-in/DailyCheckIn.tsx
  - frontend/src/components/check-in/WeeklyReview.tsx
  - frontend/src/components/training/TrainingAssessment.tsx
  - frontend/src/components/training/TrainingAnalytics.tsx
  - frontend/src/components/training/WorkoutList.tsx
  - frontend/src/components/performance/PerformanceAssessment.tsx
  - frontend/src/components/performance/PerformanceTrends.tsx
  - frontend/src/components/dashboard/PerformanceChart.tsx
  - frontend/src/components/settings/MFASetup.tsx
  - frontend/src/components/onboarding/Onboarding.tsx
  - frontend/src/components/onboarding/AthleteProfile.tsx
  - frontend/src/components/onboarding/GoalSetting.tsx
  - frontend/src/components/onboarding/TrainingPreferences.tsx
  - frontend/src/components/onboarding/InitialAssessment.tsx
  - frontend/src/components/dashboard/AthleteOverview.tsx
autonomous: true

must_haves:
  truths:
    - "Frontend compiles with zero TypeScript errors via pnpm build"
    - "Frontend runs in dev mode with pnpm dev without crashes"
    - "No runtime errors from missing imports or duplicate identifiers"
    - "All pages render without crashes (may show empty/placeholder data)"
  artifacts:
    - path: "frontend/src/components/wellbeing/Reflection.tsx"
      provides: "Clean single import block, no duplicates"
    - path: "frontend/src/components/wellbeing/WellbeingAssessment.tsx"
      provides: "Clean single import block, no duplicates"
    - path: "frontend/package.json"
      provides: "chart.js and react-chartjs-2 removed, @supabase/auth-helpers-react removed"
  key_links:
    - from: "all 20+ components using useSupabaseClient()"
      to: "frontend/src/providers/SupabaseProvider.tsx"
      via: "useSupabase() hook import"
      pattern: "import.*useSupabase.*from.*providers/SupabaseProvider"
---

<objective>
Fix all frontend compilation errors so the Next.js app builds and runs without crashes.

Purpose: The frontend currently cannot compile due to duplicate imports, broken modules, and a missing auth provider. Nothing else in Phase 1 can be verified without a running frontend.

Output: A frontend that compiles (`pnpm build` succeeds) and runs (`pnpm dev` serves pages without crashes).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken imports and remove dead dependencies</name>
  <files>
    frontend/package.json
    frontend/src/components/wellbeing/Reflection.tsx
    frontend/src/components/wellbeing/WellbeingAssessment.tsx
    frontend/src/app/demo/page.tsx
    frontend/src/utils/api.ts
    frontend/src/components/training/TrainingAnalytics.tsx
    frontend/src/components/dashboard/PerformanceChart.tsx
    frontend/src/components/performance/PerformanceTrends.tsx
  </files>
  <action>
    1. Remove dead npm packages from frontend:
       ```bash
       cd frontend
       pnpm remove chart.js react-chartjs-2 @supabase/auth-helpers-react
       ```

    2. Fix `Reflection.tsx` - This file has catastrophic duplicate imports:
       - Line 6: `'use client'` after imports (must be first line)
       - Lines 26-27: duplicate `Divider, Select` inside the import block
       - Line 34: stray `}` and second import block starting at line 34-38
       - Lines 35-38: duplicate `Calendar`, `useDisclosure`, `useSupabaseClient` imports
       - Fix: Rewrite the entire import section. Move `'use client'` to line 1. Create ONE clean import block from `@mantine/core`, ONE from `@mantine/dates`, ONE from `@mantine/hooks`, ONE from `@mantine/notifications`. Replace `useSupabaseClient` import with `import { useSupabase } from '@/providers/SupabaseProvider'`. Read the full file to understand which imports are actually used, then write a clean version.

    3. Fix `WellbeingAssessment.tsx` - Similar catastrophic duplicate imports:
       - Lines 4 AND 9: two `'use client'` directives
       - Lines 36-39: bare identifiers outside an import block (`Text, Textarea, Title, useMantineTheme`)
       - Lines 40-49: second import block duplicating mantine core, notifications, icons, and React
       - Fix: Same approach as Reflection.tsx. Read the full file, rewrite with one clean import section. Replace `useSupabaseClient` with `useSupabase`. Keep the `supabase` import from `@/lib/supabase` since WellbeingAssessment uses BOTH patterns.

    4. Fix demo page (`frontend/src/app/demo/page.tsx`):
       - Lines 23-26 import from 4 non-existent modules: `@/lib/mockData/sessionData`, `@/lib/notifications/toast`, `@/lib/storage/localStorage`, `@/lib/types/training`
       - Solution: Replace the entire file with a simple placeholder page that says "Demo page - coming soon" using Mantine components. Do NOT try to create the missing modules -- they are not needed for foundation.

    5. Delete the duplicate API client: `frontend/src/utils/api.ts`
       - This is a duplicate of `frontend/src/lib/api.ts` with auth interceptor commented out
       - Any files importing from `@/utils/api` must be updated to import from `@/lib/api` instead
       - Search for imports: `grep -r "from.*@/utils/api" frontend/src/` and update each

    6. Fix chart.js imports in 3 components that use `react-chartjs-2` (since we removed chart.js):
       - `frontend/src/components/training/TrainingAnalytics.tsx`: Replace chart.js charts with recharts equivalents or placeholder text
       - `frontend/src/components/dashboard/PerformanceChart.tsx`: Same approach
       - `frontend/src/components/performance/PerformanceTrends.tsx`: Same approach
       - For each: Read the full file, identify what the chart.js component rendered, replace with either a recharts equivalent (if simple) or a Mantine `Paper` with `Text` saying "Chart placeholder - will use recharts" (if the chart is complex). Do NOT spend time building perfect chart replacements -- just make them compile.
  </action>
  <verify>
    Run `cd frontend && pnpm tsc --noEmit 2>&1 | head -50` to check for TypeScript errors. Zero errors expected from the files modified in this task.
  </verify>
  <done>
    The six files with broken imports/missing modules compile without errors. chart.js and react-chartjs-2 are removed from package.json. `frontend/src/utils/api.ts` is deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all useSupabaseClient() with useSupabase() across 20+ components</name>
  <files>
    All 20+ components listed in files_modified that import from @supabase/auth-helpers-react
  </files>
  <action>
    The `@supabase/auth-helpers-react` package was removed in Task 1. Now 20+ components that import `useSupabaseClient` from it will fail to compile. Each must be migrated to use the existing `useSupabase()` hook from `@/providers/SupabaseProvider`.

    Migration pattern for each file:
    ```
    // BEFORE:
    import { useSupabaseClient } from '@supabase/auth-helpers-react';
    // ... later in component:
    const supabase = useSupabaseClient();

    // AFTER:
    import { useSupabase } from '@/providers/SupabaseProvider';
    // ... later in component:
    const { supabase } = useSupabase();
    ```

    Note: `useSupabase()` returns `{ supabase, loading }` where `supabase` can be `null`. Components should handle the null case with an early return:
    ```typescript
    const { supabase, loading } = useSupabase();
    if (loading || !supabase) return null; // or a loading skeleton
    ```

    Files to update (find with grep for `@supabase/auth-helpers-react`):
    1. `frontend/src/components/dashboard/AthleteDashboard.tsx`
    2. `frontend/src/components/dashboard/CoachDashboard.tsx`
    3. `frontend/src/components/dashboard/AdminDashboard.tsx`
    4. `frontend/src/components/dashboard/AthleteOverview.tsx`
    5. `frontend/src/components/wellbeing/RecoveryHealth.tsx`
    6. `frontend/src/components/wellbeing/PhysicalMetrics.tsx`
    7. `frontend/src/components/wellbeing/MoodTracking.tsx`
    8. `frontend/src/components/wellbeing/Reflection.tsx` (already handled in Task 1, verify)
    9. `frontend/src/components/wellbeing/WellbeingAssessment.tsx` (already handled in Task 1, verify)
    10. `frontend/src/components/check-in/DailyCheckIn.tsx`
    11. `frontend/src/components/check-in/WeeklyReview.tsx`
    12. `frontend/src/components/training/TrainingAssessment.tsx`
    13. `frontend/src/components/performance/PerformanceAssessment.tsx`
    14. `frontend/src/components/settings/MFASetup.tsx`
    15. `frontend/src/components/onboarding/Onboarding.tsx`
    16. `frontend/src/components/onboarding/AthleteProfile.tsx`
    17. `frontend/src/components/onboarding/GoalSetting.tsx`
    18. `frontend/src/components/onboarding/TrainingPreferences.tsx`
    19. `frontend/src/components/onboarding/InitialAssessment.tsx`

    IMPORTANT: Some of these files (especially dashboard components) are 700-1400 lines. Do NOT rewrite them. Only change the import line and the `const supabase = useSupabaseClient()` line + add the null check. Read enough of each file to find the import and usage, then make surgical edits.

    After all files are updated, run a final grep to confirm zero remaining references:
    ```bash
    grep -r "auth-helpers-react" frontend/src/
    ```
    This should return nothing.
  </action>
  <verify>
    1. `grep -r "auth-helpers-react" frontend/src/` returns no results
    2. `cd frontend && pnpm build` completes successfully (may have warnings, but no errors)
    3. `cd frontend && pnpm dev` starts the dev server without immediate crashes
  </verify>
  <done>
    All 20+ components use `useSupabase()` from the custom provider. `@supabase/auth-helpers-react` has zero references in the codebase. `pnpm build` succeeds. `pnpm dev` starts without crashes.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm build` -- must succeed with exit code 0
2. `cd frontend && pnpm dev` -- must start dev server on port 3000
3. `grep -r "auth-helpers-react" frontend/src/` -- must return nothing
4. `grep -r "chart.js\|react-chartjs-2" frontend/src/` -- must return nothing
5. `grep -r "@/utils/api" frontend/src/` -- must return nothing
6. `ls frontend/src/utils/api.ts` -- must not exist
</verification>

<success_criteria>
- `pnpm build` exits 0
- `pnpm dev` starts without crashes
- Zero references to removed packages in source code
- All pages accessible in browser without white-screen crashes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
