---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - docker-compose.yml
  - backend/Dockerfile
  - frontend/Dockerfile
  - backend/app/main.py
autonomous: false

must_haves:
  truths:
    - "docker-compose up builds all three services without errors"
    - "Frontend container serves pages on localhost:3000"
    - "Backend container responds to health check on localhost:8000"
    - "Backend container connects to Supabase and returns real data"
    - "Full stack works: frontend can call backend API and get responses"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Working Docker Compose config with frontend, backend, and db services"
    - path: "backend/Dockerfile"
      provides: "Python-based Dockerfile that builds successfully"
    - path: "frontend/Dockerfile"
      provides: "Next.js Dockerfile using pnpm that builds successfully"
  key_links:
    - from: "frontend (port 3000)"
      to: "backend (port 8000)"
      via: "NEXT_PUBLIC_API_URL=http://backend:8000"
      pattern: "NEXT_PUBLIC_API_URL"
    - from: "backend"
      to: "Supabase"
      via: "SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY env vars"
      pattern: "SUPABASE_URL"
---

<objective>
Fix Docker Compose so `docker-compose up` builds and starts all services successfully, with the frontend able to call the backend API.

Purpose: The Docker environment is broken because the backend Dockerfile is actually a Node.js Dockerfile, and the compose config needs to pass Supabase credentials properly. A working Docker environment is the final success criterion for Phase 1.

Output: `docker-compose up` starts frontend (port 3000), backend (port 8000), and PostgreSQL (port 5432) without errors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@docker-compose.yml
@backend/Dockerfile
@frontend/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Docker configuration and verify full stack</name>
  <files>
    docker-compose.yml
    backend/Dockerfile
    frontend/Dockerfile
    backend/app/main.py
  </files>
  <action>
    1. **Verify backend Dockerfile** (should have been fixed in Plan 02):
       - Confirm it starts with `FROM python:3.11-slim` (not `FROM node:`)
       - Confirm it runs `pip install -r requirements.txt`
       - Confirm CMD is `uvicorn app.main:app --host 0.0.0.0 --port 8000`
       - If Plan 02 did not fix it, fix it now

    2. **Verify frontend Dockerfile**:
       - Read `frontend/Dockerfile` -- it currently uses `pnpm` which is correct for the backend Dockerfile (confusingly named) but the ACTUAL frontend Dockerfile at `frontend/Dockerfile` uses `npm ci` and `npm run build`
       - Fix to use pnpm:
         ```dockerfile
         FROM node:20-alpine AS base
         WORKDIR /app
         RUN corepack enable && corepack prepare pnpm@9 --activate

         FROM base AS deps
         COPY package.json pnpm-lock.yaml* ./
         RUN pnpm install --frozen-lockfile || pnpm install

         FROM base AS builder
         WORKDIR /app
         COPY --from=deps /app/node_modules ./node_modules
         COPY . .
         ENV NEXT_TELEMETRY_DISABLED=1
         # Build-time env vars (will be replaced at runtime for production)
         ARG NEXT_PUBLIC_SUPABASE_URL
         ARG NEXT_PUBLIC_SUPABASE_ANON_KEY
         ARG NEXT_PUBLIC_API_URL
         RUN pnpm run build

         FROM base AS runner
         WORKDIR /app
         ENV NODE_ENV=production
         ENV NEXT_TELEMETRY_DISABLED=1
         RUN addgroup --system --gid 1001 nodejs && adduser --system --uid 1001 nextjs
         COPY --from=builder /app/public ./public
         COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
         COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
         USER nextjs
         EXPOSE 3000
         ENV PORT=3000
         ENV HOSTNAME=0.0.0.0
         CMD ["node", "server.js"]
         ```
       - NOTE: The standalone output requires `output: "standalone"` in next.config.js. Read `frontend/next.config.js` and add it if missing:
         ```js
         output: "standalone",
         ```

    3. **Update docker-compose.yml**:
       - The current compose file is mostly correct but needs Supabase env vars for the backend
       - Update the backend service to pass through all needed env vars from host `.env`:
         ```yaml
         backend:
           build:
             context: ./backend
             dockerfile: Dockerfile
           ports:
             - "8000:8000"
           volumes:
             - ./backend:/app
           environment:
             - ENVIRONMENT=development
             - DATABASE_URL=postgresql://postgres:postgres@db:5432/bobsleigh
             - SUPABASE_URL=${SUPABASE_URL}
             - SUPABASE_KEY=${SUPABASE_KEY}
             - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
             - SECRET_KEY=${SECRET_KEY:-dev-secret-key}
             - CORS_ORIGINS=http://localhost:3000,http://frontend:3000
           depends_on:
             - db
         ```
       - Update the frontend service to pass Supabase env vars:
         ```yaml
         frontend:
           build:
             context: ./frontend
             dockerfile: Dockerfile
             args:
               NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL:-${SUPABASE_URL}}
               NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY:-${SUPABASE_KEY}}
               NEXT_PUBLIC_API_URL: http://localhost:8000
           ports:
             - "3000:3000"
           environment:
             - NODE_ENV=development
             - NEXT_PUBLIC_API_URL=http://localhost:8000
             - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL:-${SUPABASE_URL}}
             - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY:-${SUPABASE_KEY}}
           depends_on:
             - backend
         ```
       - Keep the `db` service as-is (postgres:16-alpine)
       - Remove the volumes mount for frontend if it causes issues with the standalone build (volume mounts override the built output)

    4. **Enhance health check** in `backend/app/main.py`:
       Add a database connection check to the health endpoint so we can verify the backend connects to Supabase:
       ```python
       @app.get("/health")
       async def health_check():
           """Health check endpoint with database connectivity test"""
           db_status = "disconnected"
           try:
               from app.db.session import get_supabase
               sb = get_supabase()
               result = sb.table("sports").select("id").limit(1).execute()
               db_status = "connected" if result.data else "empty"
           except Exception as e:
               db_status = f"error: {str(e)}"

           return {
               "status": "healthy",
               "environment": settings.ENVIRONMENT,
               "database": db_status
           }
       ```

    5. **Test the full stack**:
       ```bash
       docker-compose build
       docker-compose up -d
       # Wait for services to start
       sleep 10
       # Check health
       curl http://localhost:8000/health
       # Check athletes endpoint
       curl http://localhost:8000/api/athletes/
       # Check frontend responds
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000
       docker-compose down
       ```
  </action>
  <verify>
    1. `docker-compose build` completes without errors for all 3 services
    2. `docker-compose up -d` starts all 3 containers
    3. `curl http://localhost:8000/health` returns `{"status":"healthy","database":"connected"}` (or "empty")
    4. `curl http://localhost:8000/api/athletes/` returns JSON (empty array or data)
    5. `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` returns 200
    6. `docker-compose down` shuts down cleanly
  </verify>
  <done>
    docker-compose up builds and starts all three services. Backend connects to Supabase. Frontend serves pages. Health check confirms database connectivity.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 foundation: frontend compiles, backend connects to Supabase, Docker Compose works.
  </what-built>
  <how-to-verify>
    1. Run `docker-compose up` and wait for all services to start
    2. Open http://localhost:3000 -- you should see the app (login page or landing page). It should NOT crash with a white screen.
    3. Open http://localhost:8000/docs -- you should see the Swagger API documentation
    4. Open http://localhost:8000/health -- should show `{"status":"healthy","database":"connected"}`
    5. In Swagger, try GET /api/athletes/ -- should return `[]` or athletes from database
    6. In Swagger, try POST /api/athletes/ with body `{"first_name":"Test","last_name":"User","email":"verify@test.com","sport":"Bobsleigh"}` -- should create an athlete
    7. Check your Supabase Dashboard -> Table Editor -> athletes table -- the new athlete should appear
    8. Run `docker-compose down` when done
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
1. `docker-compose build` succeeds for all services
2. `docker-compose up -d` starts all containers
3. `curl http://localhost:8000/health` shows database connected
4. `curl http://localhost:8000/api/athletes/` returns JSON from database
5. `curl http://localhost:3000` returns HTTP 200
6. Frontend loads in browser without white-screen crash
7. Creating an athlete via API shows up in Supabase Dashboard
</verification>

<success_criteria>
- docker-compose up starts all 3 services without errors
- Frontend serves pages on port 3000
- Backend connects to Supabase and returns real data on port 8000
- Health endpoint confirms database connectivity
- Full round-trip works: create athlete via API -> appears in Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
